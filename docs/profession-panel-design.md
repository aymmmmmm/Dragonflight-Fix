# 专业技能面板 UI 重构设计

> 日期：2026-04-13 | 文件：`modules/panels/tradeskill.lua`

## 一、设计目标

1. **KillFrame 全新搭建** — 不换皮原生面板，完全自建 UI（参考 SpellBook 模式）
2. **统一 TradeSkill + Craft** — 一个面板处理所有专业，通过 Tab 切换
3. **书本风格** — 与 SpellBook 视觉统一（PaperDollFrame 金属边框 + 羊皮纸内页）
4. **功能完整** — 保留所有原生功能：配方列表/详情/材料/制作/搜索/过滤

## 二、整体框架

```
750 x 530 px，缩放 0.9，可拖动
┌─[专业图标]────── Tailoring ──────────────────────[X]─┐  ← PaperDollFrame 金属边框
│  ┌──────── 熟练度条 RankBar (690x16) ────────┐       │
│  │  ██████████████░░░░░░░  Expert 225/300    │       │
│  └───────────────────────────────────────────┘       │
│  ┌─────────────────────┬─────────────────────┐       │
│  │                     │                     │       │
│  │   左页 — 配方列表    │   右页 — 配方详情    │       │
│  │   365 x 400         │   365 x 400         │       │
│  │                     │                     │       │
│  │                     │                     │       │
│  │                     │                     │       │
│  │                     │                     │       │
│  │                     │                     │       │
│  │                     │                     │       │
│  │                     │                     │       │
│  │                     │                     │       │
│  └─────────────────────┴─────────────────────┘       │
│  [搜索框]          [☑有材料] [☑可学]    [制作] [取消] │
├─[锻造]─[裁缝]─[烹饪]─[急救]─[附魔]─[宠物训练]───────┤  ← Tab 在底部外侧
└──────────────────────────────────────────────────────┘
```

## 三、详细布局

### A. 顶部区域

```
┌─[专业图标 52x52]──────── Tailoring ───────────────────[X 关闭]─┐
│  TOPLEFT (0,3)          TOP (0,-6)              TOPRIGHT (0,-1) │
│                                                                  │
│  ┌─ RankBar ──────────────────────────────────────────────────┐  │
│  │  TOPLEFT (30, -38)                   TOPRIGHT (-30, -38)   │  │
│  │  高度 16px | 岩石背景 + 金色进度条 + 文字居中               │  │
│  │  "Expert 225 / 300"  颜色: 金棕色 (0.78, 0.57, 0.16)      │  │
│  └────────────────────────────────────────────────────────────┘  │
```

### B. 左页 — 配方列表

```
365 x ~400
┌─────────────────────────────────────┐
│ [±] 全部展开/折叠                    │
│ ┌─────────────────────────────────┐ │
│ │ ▸ 武器 ─────────────────────── │ │  ← 分类标题 (可折叠)
│ │   · 铜斧             ████ (灰) │ │  ← 配方行: 名称 + 难度颜色
│ │   · 铜剑             ████ (绿) │ │
│ │   · 青铜阔剑         ████ (黄) │ │
│ │   · 秘银重斧         ████ (橙) │ │
│ │ ▸ 护甲 ─────────────────────── │ │
│ │   · 铜链甲           ████ (灰) │ │
│ │   · 铜腰带           ████ (绿) │ │
│ │                                 │ │
│ │                                 │ │
│ │                          ▲      │ │
│ │                       [滚动条]   │ │
│ │                          ▼      │ │
│ └─────────────────────────────────┘ │
│ [☐分类筛选▼]  [☐装备栏位▼]         │  ← 下拉筛选框
└─────────────────────────────────────┘

配方行结构 (每行 ~16px 高):
┌──────────────────────────────────────┐
│ [±]  配方名称                  [难度] │
└──────────────────────────────────────┘

难度颜色:
  - 橙色 optimal  (1.0, 0.5, 0.1): 必定提升
  - 黄色 medium   (1.0, 1.0, 0.0): 可能提升
  - 绿色 easy     (0.25, 0.75, 0.25): 偶尔提升
  - 灰色 trivial  (0.5, 0.5, 0.5): 不再提升
```

### C. 右页 — 配方详情

```
365 x ~400
┌─────────────────────────────────────┐
│                                     │
│  [物品图标 36x36]  物品名称          │  ← 品质颜色文字
│   actives_border                    │
│                                     │
│  冷却时间: 1天                       │  ← 浅棕色 (若有)
│  需求: 铁匠锤                        │  ← 浅棕色 (若有)
│  花费: 15 训练点                     │  ← 浅棕色 (宠物训练专用)
│                                     │
│  描述文本 (若有)                     │  ← 浅棕色
│  ──────────────────────────────────  │
│  材料:                               │  ← 深棕色标签
│  ┌──────────────┐ ┌──────────────┐  │
│  │ [🟫] 铜锭    │ │ [🟫] 弱效   │  │  ← 材料格: 图标+名称+数量
│  │      (4/4) ✓ │ │   助熔剂    │  │     足够=白色，不足=红色
│  └──────────────┘ │      (0/1) ✗ │  │
│  ┌──────────────┐ └──────────────┘  │
│  │ [🟫] 粗糙的  │                   │
│  │   磨刀石     │                   │
│  │      (2/2) ✓ │                   │
│  └──────────────┘                   │
│                                     │
│           [-] [数量] [+]            │  ← 数量控制 (TradeSkill only)
│       [全部制作]  [制作]  [取消]     │  ← 操作按钮
└─────────────────────────────────────┘

材料格结构 (每格 ~155x40):
┌───────────────────────┐
│ [图标   ]  材料名称    │  ← 足够=白色 / 不足=红色
│ [36x36  ]  (2/4)      │  ← 已有/需要
│ [边框   ]             │
└───────────────────────┘
```

### D. 底部区域

```
│  [🔍 搜索...]              [☑有材料] [☑可学]   [制作][取消]     │
│  BOTTOMLEFT (15, 8)                          BOTTOMRIGHT        │
│  暗色圆角 220x22                                                │
├─[锻造]─[裁缝]─[烹饪]─[急救]─[附魔]─[宠物训练]─────────────────-┤
│  Tab 系统: AddTab(), 底部外侧                                   │
│  动态生成: 监听 TRADE_SKILL_SHOW/CRAFT_SHOW 识别当前专业         │
│  宽度: 自动 (文字宽度 + 50px)                                    │
└──────────────────────────────────────────────────────────────────┘
```

## 四、配色方案

沿用 SpellBook 羊皮纸书本风格:

| 元素 | RGB | 说明 |
|------|-----|------|
| 配方名称 (列表) | 难度颜色 | 橙/黄/绿/灰 |
| 分类标题 | `(0.30, 0.17, 0.06)` | 深棕加粗 |
| 物品名称 (详情) | 品质颜色 | 白/绿/蓝/紫/橙 |
| 材料/描述文字 | `(0.50, 0.35, 0.18)` | 浅棕色 |
| 熟练度条文字 | `(0.78, 0.57, 0.16)` | 金棕色 |
| 标题 | `(0.94, 0.75, 0.38)` | 金色 |
| 复选框标签 | `(0.9, 0.9, 0.9)` | 白色 (边框区域) |

## 五、纹理清单

复用 SpellBook 已有纹理:

| 纹理 | 用途 |
|------|------|
| `spellbook_left_page.blp` / `spellbook_right_page.blp` | 左右页背景 |
| `spellbook_top_wood.blp` | 顶部木纹装饰 |
| `spellbook_bookmark.blp` | 书签装饰 |
| `spellbook_actives_border.blp` | 物品图标边框 |
| `spellbook_highlight.blp` | 悬浮高亮 |

## 六、Tab 系统

动态生成，监听事件识别专业:

```
TRADE_SKILL_SHOW → 记录当前专业 (GetTradeSkillLine())
CRAFT_SHOW       → 记录当前专业 (附魔/宠物训练/毒药)

Tab 排列: 按检测到的专业动态添加
点击 Tab → CastSpell(spellIndex, BOOKTYPE_SPELL) 触发原生事件
当前专业 Tab 高亮
```

## 七、TradeSkill vs Craft API 差异

| API | TradeSkill | Craft |
|-----|-----------|-------|
| 获取配方数 | `GetNumTradeSkills()` | `GetNumCrafts()` |
| 获取配方信息 | `GetTradeSkillInfo(i)` | `GetCraftInfo(i)` |
| 获取材料数 | `GetTradeSkillNumReagents(i)` | `GetCraftNumReagents(i)` |
| 获取材料信息 | `GetTradeSkillReagentInfo(i,j)` | `GetCraftReagentInfo(i,j)` |
| 制作 | `DoTradeSkill(i, num)` | `DoCraft(i)` |
| 冷却 | `GetTradeSkillCooldown(i)` | `GetCraftCooldown(i)` |
| 描述 | `GetTradeSkillDescription(i)` | `GetCraftDescription(i)` |
| 数量控制 | 有 (Decrement/Increment/InputBox/CreateAll) | 无 (单次制作) |
| 训练点数 | 无 | `GetPetTrainingPoints()` |
| 展开事件 | `TRADE_SKILL_SHOW` / `TRADE_SKILL_CLOSE` | `CRAFT_SHOW` / `CRAFT_CLOSE` |

## 八、数据流

```
用户打开专业 (快捷键/图标)
  ↓
原生事件 TRADE_SKILL_SHOW 或 CRAFT_SHOW
  ↓
OnEvent 处理器:
  1. 隐藏原生面板
  2. 识别当前专业类型 (TradeSkill / Craft)
  3. 获取专业名称、图标、熟练度
  4. 更新 Tab 高亮
  5. 收集配方数据 → filteredRecipes[]
  6. 渲染左页配方列表
  7. 选中第一个配方 → 渲染右页详情
  ↓
用户交互:
  - 点击配方 → 更新右页详情
  - 点击 Tab → CastSpell 切换专业 → 新的 SHOW 事件
  - 搜索/过滤 → 重新筛选 filteredRecipes → 刷新列表
  - 制作 → DoTradeSkill / DoCraft
  - 关闭 → Hide 面板
```

## 九、验证计划

1. 打开任意 TradeSkill 专业 → 新面板显示，原生面板隐藏
2. 打开 CraftFrame 专业 (附魔/宠物训练) → 同一面板显示，Tab 切换
3. 配方列表正确显示分类、折叠、难度颜色
4. 选中配方 → 右页显示物品图标、名称、材料、需求
5. 制作按钮功能正常 (单个制作、批量制作、全部制作)
6. 搜索框过滤配方列表
7. 复选框过滤 (有材料/可学) 正常
8. Tab 切换专业正常
9. 宠物训练显示训练点数
10. 熟练度条正确显示等级/进度
11. ESC 关闭、拖动、音效正常
